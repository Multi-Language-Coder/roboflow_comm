[build]
  command = "npm run build"
  publish = "dist/roboflow_communicator/browser"

# Single-page app: serve index.html for client-side routes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
